import { defineConfig } from 'vite'

export default defineConfig({
  base: '/ReactTODO/',  // Replace with your actual repo name
  build: {
    outDir: 'dist',  // Folder where the build files will be placed
  },
})
